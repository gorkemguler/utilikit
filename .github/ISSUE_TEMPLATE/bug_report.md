---
name: Bug report
about: Something isn't working
labels: bug
---

**Endpoint / tool**
`GET /v1/...`

**What happened**

**Expected**

**Repro** (a `curl` if possible)
```
curl -s 'http://localhost:8400/v1/...'
```

**Environment**
- `utilikit version`:
- install: pip / docker / from source
- Python:
- OS / board:
- optional extras installed: browser? decode? geo?
